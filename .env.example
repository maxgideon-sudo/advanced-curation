# Advanced Curation Environment Variables Template
# Copy this file to .env and fill in your actual values

# Database Configuration
# For local development, use Prisma local postgres or your own PostgreSQL instance
DATABASE_URL="postgresql://username:password@localhost:5432/advancedcuration"

# For Neon (production), use format:
# DATABASE_URL="postgresql://username:password@ep-hostname.us-east-1.aws.neon.tech/dbname?sslmode=require"

# NextAuth.js Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-super-secret-key-change-this-in-production-make-it-long-and-random"

# Application Configuration
NODE_ENV="development"
APP_URL="http://localhost:3000"

# Email Configuration (for contact form)
# You'll need to set these in your deployment environment (Vercel, etc.)
EMAIL_USER="max@advancedcuration.com"
EMAIL_PASSWORD="your-outlook-password"

# Note: For Outlook/Office 365 (GoDaddy hosted), you'll need to:
# 1. Use your full email address as EMAIL_USER
# 2. Use your regular email password as EMAIL_PASSWORD
# 3. Ensure SMTP is enabled in your Outlook settings
# 4. If using 2FA, you may need to generate an app-specific password

# Alternative email services:
# - For Gmail: Use Gmail SMTP with app passwords
# - For SendGrid: Use SENDGRID_API_KEY
# - For Mailgun: Use MAILGUN_API_KEY and MAILGUN_DOMAIN
# - For AWS SES: Use AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY

# Production Environment Variables (set these in Vercel/your hosting platform)
# NEXTAUTH_URL="https://your-domain.vercel.app"
# DATABASE_URL="your-production-database-url"
# NODE_ENV="production"
# EMAIL_USER="your-production-email@gmail.com"
# EMAIL_PASSWORD="your-production-app-password"